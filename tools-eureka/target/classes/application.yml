
server:  # ????
  port: 9090
spring:
  application:  # ?????eureka ????????id
    name: eureka-server
eureka:
  instance:
    hostname: localhost
  client:
    service-url:   #  eureka server ???? ?????????????
      defaultZone:  http://localhost:9090/eureka
    register-with-eureka: false  # ??eureka server ????
    fetch-registry: false  # ??eureka server ??????
